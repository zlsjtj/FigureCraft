# 1.15.0 增量验收记录

2026-09-26。将“质量升级必须换构图”改为按实际缺口选择重构或局部精修。新增外来 SVG 的有限实色文字对比筛查；已有字号和直线碰撞接口保持兼容。

本轮实际执行：

- `tests/test_svg_contrast.py`：15 项通过。覆盖白底/深色面、继承颜色、无画布、未知/局部/后画遮挡、透明、缺失或重复 ID、非有限几何等。
- `tests/run_svg_label_tests.py`：既有 17 项通过。
- 真实机制图旧版正确检出三处文字在白底的对比为 4.0983:1；只把这些文字加深后为 5.4227:1，复查通过。文本、几何、数值、线和其他颜色不变。
- [原创层板例](../examples/plate-labels/README.md)：两处浅字被检出并修复，深色面内的白字以该面为背景正确通过。前后源与导出均保留。
- 搬移完整技能和固定源码后，从另一工作目录重建，两例合计 11 个 SVG/PDF/PNG 与选定产物逐字节一致。仍是同一操作系统与运行时。

文字筛查必须显式提供画布颜色；只计算已度量且具有唯一 ID 的普通文字及支持的实色矩形内部背景。复杂合成、CSS、变换、透明和未知遮挡保持 `REVIEW_REQUIRED`。未给画布为 `NOT_RUN`。4.5:1 是内部筛查线，不是不可读判决或美感认证。

维护模型查看了前后图、选定机制图正常色/灰度/单一色觉模拟及实际 Word 入稿页。层板是结构不同的固定源迁移例，未新增实验性能或材料数据。本轮无新的独立盲读、盲生成或真人测试，不声称阅读时间变快。保留此前有效构图，停止无新假设的改画。

官方入口、链接及依赖另行检查。精确输出哈希和执行记录保存在本地交付包 audit/。历史引擎测试本轮未全量重跑，不把旧版全部 PASS 继承为 1.15.0 的执行结果。
